#########################################################################
# SOURCE ACQUISITION
#########################################################################
DLAGENTS=('file::/usr/bin/aria2c -UWget -s1 -x4 -j8 -k10M --retry-wait=3 --optimize-concurrent-downloads=true --event-poll=epoll --file-allocation=falloc %u -o %o --follow-metalink=mem --auto-file-renaming=false'
          'ftp::/usr/bin/aria2c -UWget -s1 -x4 -j8 -k10M --retry-wait=3 --optimize-concurrent-downloads=true --event-poll=epoll --file-allocation=falloc %u -o %o --follow-metalink=mem --auto-file-renaming=false'
          'http::/usr/bin/aria2c -UWget -s1 -x4 -j8 -k10M --retry-wait=3 --optimize-concurrent-downloads=true --event-poll=epoll --file-allocation=falloc %u -o %o --follow-metalink=mem --auto-file-renaming=false'
          'https::/usr/bin/aria2c -UWget -s1 -x4 -j8 -k10M --retry-wait=3 --optimize-concurrent-downloads=true --event-poll=epoll --file-allocation=falloc %u -o %o --follow-metalink=mem --auto-file-renaming=false'
          'rsync::/usr/bin/rsync --no-motd -z --partial %u %o'
          'scp::/usr/bin/scp -C -q %u %o')
#DLAGENTS=('file::/usr/bin/curl -qgC - -o %o %u'
#          'http::/usr/bin/curl -qgb "" -fLC - --retry 3 --retry-delay 3 -o %o %u'
#          'https::/usr/bin/curl -qgb "" -fLC - --retry 3 --retry-delay 3 -o %o %u'
#          'ftp::/usr/bin/curl -qgfC - --ftp-pasv --retry 3 --retry-delay 3 -o %o %u')

VCSCLIENTS=('bzr::breezy'
            'fossil::fossil'
            'git::git'
            'hg::mercurial'
            'svn::subversion')
#########################################################################
# ARCHITECTURE, COMPILE FLAGS
#########################################################################
CARCH="x86_64"
CHOST="x86_64-pc-linux-gnu"
PACKAGECARCH="x86_64"
# CC="clang"
# CXX="clang++"
# CPP="clang-cpp"
# LD="ld.lld"
# AR="llvm-ar"
# NM="llvm-nm"
# RANLIB="llvm-ranlib"
# STRIP="llvm-strip"

# Compiler and Linker Flags
CFLAGS="-march=native -O3 -pipe -fno-plt -fexceptions -Wp,-D_FORTIFY_SOURCE=3 \
  -Wformat -Wno-error -fstrict-aliasing -fstack-clash-protection -fcf-protection=none \
  -fno-semantic-interposition -fdata-sections -ffunction-sections -fomit-frame-pointer"
CXXFLAGS="$CFLAGS -Wp,-D_GLIBCXX_ASSERTIONS -fvisibility-inlines-hidden"
LDFLAGS="-Wl,-O3,--sort-common,--as-needed,-gc-sections,--icf=safe,--hash-style=gnu,-z,relro,-z,pack-relative-relocs,-fuse-ld=lld"
LTOFLAGS="-flto=auto"
RUSTFLAGS="-Copt-level=3 -Ctarget-cpu=native -Ccodegen-units=1 -Cdebuginfo=0 -Cstrip=symbols -Cllvm-args=-enable-dfa-jump-thread"
GOFLAGS="-ldflags=-s -w -trimpath -modcacherw -buildvcs=false"
#-- Make Flags
MAKEFLAGS="-j$(nproc)"
NINJAFLAGS="-j$(nproc)"
#-- Debugging flags
DEBUG_CFLAGS="-g0"
DEBUG_CXXFLAGS="$DEBUG_CFLAGS"

#########################################################################
# BUILD ENVIRONMENT
#########################################################################
# Makepkg defaults: BUILDENV=(!distcc !color !ccache check !sign)
#  A negated environment option will do the opposite of the comments below.
#-- distcc:   Use the Distributed C/C++/ObjC compiler
#-- color:    Colorize output messages
#-- ccache:   Use ccache to cache compilation
#-- check:    Run the check() function if present in the PKGBUILD
#-- sign:     Generate PGP signature file
BUILDENV=(!distcc color ccache !check !sign)

#-- If using DistCC, your MAKEFLAGS will also need modification. In addition,
#-- specify a space-delimited list of hosts running in the DistCC cluster.
#DISTCC_HOSTS=""
#-- Specify a directory for package building.
#BUILDDIR=/tmp/makepkg

#########################################################################
# GLOBAL PACKAGE OPTIONS
#   These are default values for the options=() settings
#########################################################################
# Makepkg defaults: OPTIONS=(!strip docs libtool staticlibs emptydirs !zipman !purge !debug !lto !autodeps)
#  A negated option will do the opposite of the comments below.
#-- strip:      Strip symbols from binaries/libraries
#-- docs:       Save doc directories specified by DOC_DIRS
#-- libtool:    Leave libtool (.la) files in packages
#-- staticlibs: Leave static library (.a) files in packages
#-- emptydirs:  Leave empty directories in packages
#-- zipman:     Compress manual (man and info) pages in MAN_DIRS with gzip
#-- purge:      Remove files specified by PURGE_TARGETS
#-- debug:      Add debugging flags as specified in DEBUG_* variables
#-- lto:        Add compile flags for building with link time optimization
#-- autodeps:   Automatically add depends/provides
OPTIONS=(strip !docs !libtool !staticlibs !emptydirs zipman purge !debug lto autodeps)

#-- File integrity checks to use. Valid: md5, sha1, sha224, sha256, sha384, sha512, b2
INTEGRITY_CHECK=(b2 sha256)
#-- Binaries: Strip all symbols, section headers, and compiler/linker junk.
STRIP_BINARIES="--strip-all -x -D --strip-section-headers -R .note.comment -R .note.gnu.gold-version -R .note.package -R .note.gnu.build-id -R .note.ABI-tag"
#-- Shared Libraries: Strip unneeded symbols, section headers, and junk.
STRIP_SHARED="--strip-unneeded -X -D --strip-section-headers -R .note.comment -R .note.gnu.gold-version -R .note.package -R .note.gnu.build-id"
#-- Static Libraries: Debug symbols only. NO stripping section headers (breaks LTO/linking).
STRIP_STATIC="--strip-debug -X -D -R .note.comment -R .note.gnu.gold-version -R .note.package -R .note.gnu.build-id"
#-- Manual (man and info) directories to compress (if zipman is specified)
MAN_DIRS=({usr{,/local}{,/share},opt/*}/{man,info})
#-- Doc directories to remove (if !docs is specified)
DOC_DIRS=(usr/{,local/}{,share/}{doc,gtk-doc} opt/*/{doc,gtk-doc})
#-- Files to be removed from all packages (if purge is specified)
PURGE_TARGETS=(usr/{,share}/info/dir .packlist *.pod)

#########################################################################
# PACKAGE OUTPUT
#########################################################################
# Default: put built package and cached source in build directory
#-- Destination: specify a fixed directory where all packages will be placed
#PKGDEST=/home/packages
#-- Source cache: specify a fixed directory where source files will be cached
#SRCDEST=/home/sources
#-- Source packages: specify a fixed directory where all src packages will be placed
#SRCPKGDEST=/home/srcpackages
#-- Log files: specify a fixed directory where all log files will be placed
#LOGDEST=/home/makepkglogs
#-- Packager: name/email of the person or organization building packages
#PACKAGER="John Doe <john@doe.com>"
#-- Specify a key to use for package signing
#GPGKEY=""

#########################################################################
# COMPRESSION DEFAULTS
#########################################################################
COMPRESSGZ=(pigz -c -f -n)
COMPRESSBZ2=(lbzip2 -c -f)
COMPRESSXZ=(pixz -p $(nproc))
COMPRESSZST=(zstd -c -z -f -T0 --auto-threads=logical -9 -)
COMPRESSLRZ=(lrzip -q)
COMPRESSLZO=(lzop -q)
COMPRESSLZ=(plzip -c -f)
COMPRESSLZ4=(lz4 -q)
COMPRESSZ=(pigz -c -f -n)
#########################################################################
# EXTENSION DEFAULTS
#########################################################################
PKGEXT='.pkg.tar.zst'
SRCEXT='.src.tar.zst'
#########################################################################
# OTHER
#########################################################################
#-- Command used to run pacman as root, instead of trying sudo and su
PACMAN_AUTH=(sudo-rs)

# vim:set sw=2 ts=2 ft=sh et:
