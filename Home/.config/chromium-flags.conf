# CHROMIUM/ELECTRON FLAGS
# -----------------------------------------------------------------------------
# Platform & UI (Wayland Native)
# -----------------------------------------------------------------------------
--ozone-platform=wayland
--ozone-platform-hint=wayland
--enable-wayland-ime
# -----------------------------------------------------------------------------
# Core Features
# -----------------------------------------------------------------------------
--enable-features=UseOzonePlatform,WaylandWindowDecorations,ParallelDownloading,CanvasOopRasterization,UseSkiaRenderer,UseAngleVulkan,VaapiVideoDecoder,VaapiVideoEncoder,WebRTCPipeWireCapturer,WebAssemblyLazyCompilation,ThrottleDisplayNoneAndVisibilityHiddenCrossOriginIframes,AcceleratedVideoDecodeLinuxGL,AcceleratedVideoEncoder,AcceleratedVideoDecoder,AcceleratedVideoDecodeLinuxZeroCopyGL,EnableDrDc,BackForwardCache:TimeToLiveInBackForwardCacheInSeconds/300/should_ignore_blocklists/true/enable_same_site/true,DeferImplInvalidation,ScrollUpdateOptimizations
--disable-features=UseChromeOSDirectVideoDecoder,WaylandWpColorManagerV1
--enable-source-maps
# -----------------------------------------------------------------------------
# GPU & Rendering
# -----------------------------------------------------------------------------
--use-gl=angle
--use-angle=vulkan
--use-cmd-decoder=passthrough
--force-high-performance-gpu
--enable-gpu-rasterization
--enable-oop-rasterization
--enable-native-gpu-memory-buffers
--enable-zero-copy
--enable-low-res-tiling
--enable-hardware-overlays=single-fullscreen,single-on-top,underlay
--num-raster-threads=8
--disable-gpu-vsync
--enable-threaded-compositing
--ignore-gpu-blocklist
# -----------------------------------------------------------------------------
# Process & Performance
# -----------------------------------------------------------------------------
--process-per-site
# -----------------------------------------------------------------------------
# Networking
# -----------------------------------------------------------------------------
--enable-quic
--enable-tls13-early-data
# -----------------------------------------------------------------------------
# Telemetry & Services
# -----------------------------------------------------------------------------
--disable-crash-reporter
--disable-logging
--disable-breakpad
--no-default-browser-check
--no-pings
# -----------------------------------------------------------------------------
# Experimental
# -----------------------------------------------------------------------------
--js-flags="--opt"
--enable-experimental-web-platform-features
