[tool.ruff]
line-length = 120
target-version = "py311"
# Enforce 2-space indent (requires Ruff v0.2.0+)
indent-width = 2

[tool.ruff.lint]
<<<<<<< Updated upstream
select = ["E", "W", "F", "I", "N", "UP", "B", "C4", "SIM", "RUF"]
ignore = ["E501"] # Line length handled by formatter
||||||| Stash base
select = [
  "E",   # pycodestyle errors
  "W",   # pycodestyle warnings
  "F",   # pyflakes
  "I",   # isort
  "N",   # pep8-naming
  "UP",  # pyupgrade
  "B",   # flake8-bugbear
  "C4",  # flake8-comprehensions
  "SIM", # flake8-simplify
  "RUF"  # ruff-specific
]
ignore = ["E501"]  # line too long (handled by formatter)
=======
select = [
  "E",   # pycodestyle errors
  "W",   # pycodestyle warnings
  "F",   # pyflakes
  "I",   # isort
  "N",   # pep8-naming
  "UP",  # pyupgrade
  "B",   # flake8-bugbear
  "C4",  # flake8-comprehensions
  "SIM", # flake8-simplify
  "RUF", # ruff-specific
]
ignore = ["E501"] # line too long (handled by formatter)
>>>>>>> Stashed changes

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"

[tool.black]
line-length = 120
target-version = ["py311"]
