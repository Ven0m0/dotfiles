# Selene Lua linter configuration
# https://kampfkarren.github.io/selene/
# Standard library to use
std = "lua51+lua52+lua53+luajit"
# Exclude patterns
exclude = [
  ".git",
  "node_modules",
  "vendor",
  ".venv",
  "dist",
  "build",
  "target",
]
